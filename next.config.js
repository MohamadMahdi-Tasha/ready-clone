// Codes by mahdi tasha
// Importing NextJS config type
/** @type {import('next').NextConfig} */

// Defining config's of NextJS
const nextConfig = {}

// Exporting the config
module.exports = nextConfig
